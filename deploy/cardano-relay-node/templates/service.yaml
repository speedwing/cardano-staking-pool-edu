apiVersion: v1
kind: Service
metadata:
  name: {{ include "cardano-relay-node.fullname" . }}
  labels:
    {{- include "cardano-relay-node.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cardano-relay-node.selectorLabels" . | nindent 4 }}
